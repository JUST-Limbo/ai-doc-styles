---
description: 对外中文技术文章（如掘金稿）：Markdown 触发 + 必读本地 .cursor/writing-guide；远端仅供人工同步参考
globs: "**/*.md"
alwaysApply: false
---

# 中文对外技术文章写作规范（入口）

**适用范围**：本规范针对准备**公开发表**的中文技术文章（例如发布在掘金等技术社区的博文、教程稿），侧重体例、标点与可读性。**不适用于**业务需求说明、接口契约、内部运维手册、纯产品 README 等以工程协作为主的 Markdown；编辑那些文件时，除非用户明确要求按本规范执行，否则不要套用本套体例。

在编写、审阅或批量修订**属于上述范围**的本仓库内中文 **Markdown** 时：体例、示例与 **AI 作业纪律**（先读目标文件、旧文不大改、改后总结、交叉引用检查、可选章节等）一律以 **`.cursor/writing-guide/chinese-tech-writing.md`** 为**单一事实源**；**本文件不在此重复列举**，以免与事实源漂移。

凡涉及体例判断或正文改写，**必须先**用 Read 读取该文件**当前全文**再动手；不要仅凭记忆或旧对话摘要套用细则。若同一会话内已读过且你确认该文件未被他人离线改写，可不再重复 Read，但仍须以目标 `.md` 的当前内容为准。

**本仓库地址（规范发布源，供人工同步参考）**：写作规范事实源为本仓库内的 **`.cursor/writing-guide/chinese-tech-writing.md`**。本仓库远端为 **[https://github.com/JUST-Limbo/ai-doc-styles](https://github.com/JUST-Limbo/ai-doc-styles)**；需要拿到「已发布到远端」的最新版时，由维护者在本机执行 `git pull origin main` 或在新目录 `git clone` 后再阅读上述路径即可。**本条仅为说明文字，不触发任何自动联网或 git 操作**；Agent 在对话中**默认只 Read 工作区内的该文件**，除非用户明确要求，否则不要为「探查最新规范」而执行 git 或访问 GitHub。体例条文的历史整合与许可说明见事实源文件文首，此处不重复。

**编排**（何时强调全文、何时可按章节侧重 Read）：见该文件中的 **「给 Agent 的读取与编排」**（在文首摘要段之后、体例各章之前）。**作业纪律**：见同文件 **「AI操作文档注意事项」**。
